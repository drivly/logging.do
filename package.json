{
  "name": "logging.do",
  "version": "1.0.0",
  "description": "Durable Object Logging API",
  "main": "worker.js",
  "repository": "git@github.com:drivly/logging.do.git",
  "author": "Samuel Lippert <samuel@driv.ly>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "dev": "wrangler dev worker.js --local true",
    "deploy": "wrangler publish worker.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "devDependencies": {
    "jest": "^29.0.1",
    "prettier": "^2.7.1",
    "wrangler": "^2.0.28"
  }
}
